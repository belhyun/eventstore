<%= form_for(@group_product) do |f| %>
  <% if @group_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_product.errors.count, "error") %> prohibited this group_product from being saved:</h2>

      <ul>
      <% @group_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
